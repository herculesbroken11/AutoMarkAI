diff --git a/node_modules/@ffmpeg-installer/ffmpeg/index.js b/node_modules/@ffmpeg-installer/ffmpeg/index.js
index 80d8544..648c3b4 100644
--- a/node_modules/@ffmpeg-installer/ffmpeg/index.js
+++ b/node_modules/@ffmpeg-installer/ffmpeg/index.js
@@ -1,5 +1,9 @@
 var path = require('path');
 var os = require('os');
+var url = require('url');
+
+const isESM = true;
+const __filename = url.fileURLToPath(import.meta.url);
 
 var platform = os.platform();
 if (platform !== 'darwin' && platform !== 'linux' && platform !== 'win32') {
@@ -10,7 +14,10 @@
 
 var arch = os.arch();
 
-var ffmpegPath = path.join(
+var ffmpegPath = isESM ?
+path.join(
+  path.dirname(__filename),
+  'node_modules', '@ffmpeg-installer', platform + '-' + arch, 'ffmpeg'
+) : path.join(
   __dirname,
   '..',
   '@ffmpeg-installer',
